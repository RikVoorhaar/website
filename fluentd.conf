<source>
  @type forward
  port 24224
</source>

<match nginx.**>
  @type mysql
  host mysql
  port 3306
  username root
  password password
  database logs
  table nginx
  buffer_type file
  buffer_path /var/log/fluentd/mysql
  flush_interval 10s
  encoding utf8
</match>
